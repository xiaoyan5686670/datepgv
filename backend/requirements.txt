fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
# MySQL execute + connection test (data连接设置)
aiomysql==0.2.0
PyMySQL>=1.1.0,<2
pgvector==0.3.6
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
python-multipart==0.0.20

# Auth (JWT + password hashing)
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# LLM — DashScope/通义需 litellm>=1.75（1.57 无 dashscope provider，会报 LLM Provider NOT provided）
# 见 https://docs.litellm.ai/docs/providers/dashscope
# 1.83.9+ 原生支持 Ollama think 参数（无需 monkey-patch）
litellm==1.83.9

# Transitive peer pinned to match litellm 1.83.9 exact requirements
openai==2.24.0

# SQL parsing & validation
sqlfluff==3.3.1
sqlparse==0.5.3

# CSV / Excel import
openpyxl==3.1.5
pandas==2.2.3

# HTTP client — litellm 1.83.9 pins httpx==0.28.1
httpx==0.28.1

# DDL parsing
sqlglot==26.0.0

# Graph processing
networkx==3.4.2
